# C基础

### scanf（），gets（）函数区别与联系
1. scanf()方法遇到空格，换行符或EOF会终止；gets()方法空格被视为输入的一部分。
2. gets只用于读取字符串。
   

### scanf函数输入时为什么要加&
对于scanf函数，它将参数视为一个地址，将键盘输入的字符传到这个地址中去，通常我们都是用的&地址的符号，事实上如果一个值恰好是一个地址的话，不加&也一样可以的。
